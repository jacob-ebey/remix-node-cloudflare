export default function Node() {
  return (
    <p>
      Since <code>/layout/node</code> requested to render on the origin Node.js server,
      the common layout is also rendering on the origin.
    </p>
  );
}
