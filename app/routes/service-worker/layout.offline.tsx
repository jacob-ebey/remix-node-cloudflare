export default function Offline() {
  return (
    <p>
      Since <code>/layout/offline</code> requested to render offline in the Web
      Worker, the common layout is also rendering offline in the worker.
    </p>
  );
}
